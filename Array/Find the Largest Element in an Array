***** Notes *****
Brute Force => Sort the array and return the End value

Optimal => 
- Declare "max" variable and traverse
- Update max incase of any maximum values

***** Solution *****
def largestElement(arr: [], n: int) -> int:
    max = arr[0]
    for i in range(0, n):
        if max < arr[i]:
            max = arr[i]
    return max
