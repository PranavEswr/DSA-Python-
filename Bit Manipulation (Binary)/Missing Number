***** Notes *****
Best => Find the difference between Sum of arrays or Take XOR

XOR =>
res = len(nums)
for i in range(len(nums)):
    res ^= i^nums[i]
return res

Gauss Formula n(n+1)//2 =>
exp_sum = (len(nums))*(len(nums)+1)//2
act_sum = sum(nums)
return exp_sum - act_sum
