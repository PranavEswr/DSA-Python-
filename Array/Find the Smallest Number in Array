***** Notes *****
Brute Force => Sort the array and return the first value

Optimal => 
- Declare "min" variable and traverse
- Update min incase of any minimum values

***** Solution *****
min = arr[0]
for i in range(0, n):
  if min > arr[i]:
    min = arr[i]
return min
